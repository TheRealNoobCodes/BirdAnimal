export class LifeSpan { 
    private lifespan: number

    constructor(lifespan: number) {
        this.lifespan = lifespan;
    }

    FindLifeSpan(lifespan: number): string {
        let x = lifespan 
        if (lifespan = x  )
        return "This bird's life span is" + x 

    }
}